{
  "name": "mymemguard-agent",
  "version": "0.2.8",
  "description": "Local agent for MyMemGuard — gives the browser app full file system and terminal access",
  "bin": {
    "mymemguard-agent": "./src/cli.js"
  },
  "main": "./src/cli.js",
  "engines": { "node": ">=18" },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "scripts": {
    "start": "node src/cli.js"
  }
}
